/**
 * Calculate CRC32 checksum for the given bytes.
 * @param bytes - The input bytes to compute CRC32 for
 * @returns A Uint8Array containing the CRC32 checksum
 */
export function crc32(bytes: ArrayLike<number>): Uint8Array;
